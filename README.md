This template listens map addition events and logs them.

Following placeholders are from `config.yaml`:
* `ClusterName`
* `ClusterDiscoveryToken`
* `SslCaPath`
* `SslCertPath`
* `SslKeyPath`
* `SslKeyPassword`

Following placeholders are from `defaults.yaml`:
* `MapName`
